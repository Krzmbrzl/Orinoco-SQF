dist: bionic

language: java

git:
  submodules: true

jdk:
 - openjdk11
 - oraclejdk11

before_install:
    - sudo apt update
    - sudo apt install ant
    - sudo apt install ant-optional
    - sudo apt install jflex

script:
  - java --version
  - ant -version
  - ant test
